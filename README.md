【NetTool 使用说明】

1. 环境安装（首次运行必做）：
   - 双击安装 npcap-1.79.exe (安装时必须勾选 "Install Npcap in WinPcap API-compatible Mode")。
   - 请确保电脑已安装 Python 3，并安装了 matplotlib (pip install matplotlib)。

2. 运行：
   - 右键以管理员身份运行 NetTool.exe。
